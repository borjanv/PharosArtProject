@using File = Umbraco.Web.PublishedContentModels.File
@model List<pharosArt.Models.LandingPageModel>
@{
    ViewBag.Title = "LandingPage";
}

<div class="content articles-wrapper">
    @foreach (var item in Model)
    {
        if (item.Media.DocumentTypeAlias == ContentImage.ModelTypeAlias)
        {
            <div class="small-article">
                <div class="artcile">
                    @Html.Partial("~/Views/Partials/Home/_ArticleThumbnailPartial.cshtml", new ContentImage(item.Media))
                </div>
            </div>
        }
        else if (item.Media.DocumentTypeAlias == ContentMusic.ModelTypeAlias)
        {
            <div class="small-article">
                <div class="artcile">
                    @Html.Partial("~/Views/Partials/Home/_ArticleAudioPartial.cshtml", new ContentMusic(item.Media))
                </div>
            </div>
        }
    }
</div>